<ul>
  <h3>Categories</h3>
  <% categories.each do |category| %>
    <li>
      <h4>
        <button class="category"><%= link_to category.name, category_path(category) %></button>
      </h4>
    </li>
  <% end %>
</ul>

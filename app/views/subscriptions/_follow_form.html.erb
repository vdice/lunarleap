<%= form_for [current_user, Subscription.new], html: { style: 'display:inline-block;'}, remote: true do |f| %>
  <%= f.hidden_field :author_id, value: @user.id %>
  <%= f.submit 'Follow', id: 'follow-button' %>
<% end %>
